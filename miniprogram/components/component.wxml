<!--component.wxml-->
<!-- <text>component.wxml</text> -->
<view class="fill-item">
   <block wx:for="{{titleArr}}" wx:key="item">
     <text wx:if="{{item !== '_'}}">{{item}}</text>
     <input wx:else class="fill-input" data-value="{{index}}" bindinput="fnInput"  />
   </block>
</view>